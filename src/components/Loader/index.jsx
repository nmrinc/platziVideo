import React from 'react';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';

const Loader = () => (
  <div className="loading"><FontAwesomeIcon icon="spinner" spin size="6x" /></div>
);

export default Loader;